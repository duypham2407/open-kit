## What's changed

- add a canonical machine-readable default command permission policy with routine default-allow intent and confirm-required dangerous command exceptions
- project the policy into global OpenKit kit/profile config, repo-local config, and manual install materialization surfaces
- surface command permission policy health, drift, malformed policy handling, dangerous-entry coverage, and degraded upstream OpenCode default-action support in doctor output
- document the permission boundary clearly: no prompt broker, pseudo-terminal auto-confirm, OpenCode fork/patch, or weakening of git/release safety rules

## Validation

- `npm run verify:all`
- `npm run verify:semgrep-quality`
- `npm pack --dry-run --json`
- FEATURE-960 Code Review pass and QA pass

## Published package

- npm: `@duypham93/openkit@0.3.31`

## Notes

- OpenCode support for true default-allow plus confirm-required exception semantics remains unverified, so OpenKit reports the effective policy support as degraded instead of overclaiming prompt-free behavior.
- Dangerous delete/data-loss, destructive git, publish/release/deploy, database destructive, and privileged/system-impacting commands remain confirmation-required by policy.
