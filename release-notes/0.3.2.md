## What's changed

- refactor the active workflow around `Product Lead` and `Solution Lead` while keeping split-role assets as compatibility-only surfaces
- add `Code Reviewer` as an explicit technical gate before QA in `full` and `migration` flows
- tighten role prompts, planning skills, and operator/maintainer policy guidance so scope, solution, review, and QA boundaries stay sharp
- update registry and install metadata to mark legacy split-role assets as compatibility-only

## Validation

- `node --test ".opencode/tests/*.test.js"`
- `node --test "tests/install/install-state.test.js" ".opencode/tests/*.test.js"`

## Published package

- npm: `@duypham93/openkit@0.3.2`

## Notes

- active workflow ownership now belongs to `Product Lead`, `Solution Lead`, `Code Reviewer`, and `QA Agent`
- legacy `PM Agent`, `BA Agent`, `Architect Agent`, and `Tech Lead Agent` assets remain bundled for backward compatibility only
