## What's changed

- harden `Master Orchestrator` prompts and workflow docs so the role is orchestration-only and never writes code, patches files, or impersonates `FullstackAgent`
- make the active workflow wording explicit that `Master Orchestrator` is the boss role: route, assign, approve, reroute, and close the loop, but never implement the solution itself
- sync the stricter routing-only contract into the install-bundle mirrors and maintainer-facing AI surface guidance

## Validation

- `node --test ".opencode/tests/workflow-contract-consistency.test.js" && npm run verify:install-bundle && node --test tests/runtime/governance-enforcement.test.js`
- `npm run verify:all`

## Published package

- npm: `@duypham93/openkit@0.3.11`

## Notes

- `Master Orchestrator` remains responsible for lane choice, dispatch, state, approvals, and rerouting only
- implementation, code edits, and direct execution of the solution package stay delegated to execution roles such as `FullstackAgent`
