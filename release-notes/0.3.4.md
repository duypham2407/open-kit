## What's changed

- remove legacy split-role agents and move historical workflow material out of the active AI-facing surface
- complete the AI-first vocabulary cleanup by renaming active scope/solution commands, skills, and artifact paths
- add strict AI reading guidance plus machine-readable review and QA output schemas

## Validation

- `npm run verify:all`

## Published package

- npm: `@duypham93/openkit@0.3.4`

## Notes

- active artifact paths now use `docs/scope/` and `docs/solution/`
- active commands now use `/write-solution` and `/execute-solution`
- active skills now use `writing-scope` and `writing-solution`
- `context/core/active-contract.json` and `docs/maintainer/2026-03-26-ai-surface-map.md` define the intended AI-facing contract
