## What's changed

- migrate the checked-in `.opencode` runtime, CLI, and tests from CommonJS to native ESM
- replace brittle `require.cache` test patching with DI-friendly store override hooks in the workflow controller
- enforce ESM-safe import hygiene across runtime surfaces (`node:` builtins and explicit `.js` relative imports)
- keep migration lifecycle and multi-work-item tests aligned with new policy/tool-evidence gates

## Validation

- `node --test .opencode/tests/*.test.js`

## Published package

- npm: `@duypham93/openkit@0.3.18`

## Notes

- this release supersedes `0.3.17` publishing metadata after npm rejected a duplicate version publish
