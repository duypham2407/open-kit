## What's changed

- add explicit retained worktree launch modes for `new`, `reuse`, `reopen`, and `none`
- separate retained worktree cleanup from task completion while keeping follow-up fix context reusable
- add safe opt-in `.env` propagation with conflict-aware retained behavior and pre-launch copy warnings
- fix verify CI to install dependencies before running `verify:all`, and align runtime status/session-read behavior with the current runtime expectations

## Validation

- `npm run verify:all`
- `gh pr checks 1`

## Published package

- npm: `@duypham93/openkit@0.3.28`

## Notes

- this release improves day-to-day retained worktree UX for follow-up fixes without forcing automatic cleanup
- GitHub release metadata and version files are prepared here; npm publication still depends on maintainer credentials and publish policy outside the repository workflow
