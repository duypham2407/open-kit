## What's changed

- require Quick Agent to confirm shared understanding during `quick_brainstorm` before any solution analysis begins
- move quick-lane option analysis, user option selection, and separate plan confirmation into `quick_plan`
- add focused contract coverage and QA/scope/solution artifacts for the quick-lane workflow realignment

## Validation

- `node --test ".opencode/tests/quick-lane-contract.test.js"`
- `node --test ".opencode/tests/workflow-contract-consistency.test.js"`
- `node --test ".opencode/tests/workflow-state-controller.test.js"`
- `node .opencode/workflow-state.js doctor`
- `npm run verify:install-bundle`
- `npm run verify:governance`

## Published package

- npm: `@duypham93/openkit@0.3.27`

## Notes

- quick tasks now require explicit understanding confirmation even for tiny tasks before options or plans are shown
- this release used documented manual overrides for unavailable `tool.rule-scan` and `tool.security-scan` session tooling during review/QA, and those caveats are recorded in the QA artifact
