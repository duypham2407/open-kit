## What's changed

- harden tooling-first enforcement across shared prompt contracts, agent prompts, command prompts, and high-risk skills so repository exploration consistently prefers kit intelligence tools first
- expand `context/core/tool-substitution-rules.md` with explicit enforcement levels and add `tool.heuristic-lsp` as a documented preferred fallback for lightweight symbol navigation
- add runtime coverage for the bash guard hook and governance regression coverage that ensures agents, commands, and key skills continue to reference `tool-substitution-rules.md`
- sync the install bundle so the published global-kit surface matches the checked-in authoring surface for updated agents, commands, and skills

## Validation

- `npm run verify:install-bundle`
- `node --experimental-test-isolation=none --test tests/runtime/governance-enforcement.test.js`
- `node --experimental-test-isolation=none --test tests/runtime/bash-guard-hook.test.js`
- `node --experimental-test-isolation=none --test tests/runtime/*.test.js`

## Published package

- npm: `@duypham93/openkit@0.3.21`

## Notes

- `quick` and `full` work now document strict tooling-first behavior more explicitly, while `migration` work documents moderate fallback behavior for compatibility-oriented investigation
- governance tests now fail if critical prompts drift away from the tooling-first contract, which should reduce regression risk in future prompt and skill edits
