## What's changed

- harden codemod preview/apply so transform paths and target files are restricted to the active project root
- harden Semgrep audit execution so rule scans return structured failure states instead of throwing on spawn or parse failures
- fix security scan string-target forwarding, improve managed-kit runtime asset copying, and align action tracking with degraded and invalid tool outcomes
- correct runtime docs and test coverage around syntax scope, managed install contents, and version-sensitive CLI assertions

## Validation

- `node --test tests/runtime/codemod-tools.test.js`
- `node --test tests/runtime/audit-tools.test.js`
- `node --test tests/global/ensure-install.test.js`
- `node --test tests/runtime/runtime-platform.test.js`
- `npm run verify:all`

## Published package

- npm: `@duypham93/openkit@0.3.15`

## Notes

- codemod tools now reject transform or target paths outside the current project root with `invalid-path`
- rule-scan and security-scan now preserve structured runtime behavior when Semgrep execution or output parsing fails
- managed global installs now include `src/global/tooling.js`, reducing runtime import drift between the source repo and the installed kit
