## What's changed

- complete the package-first workflow hardening so `scope_package` and `solution_package` become first-class runtime artifacts alongside compatibility slots
- tighten runtime validation, governance checks, and shipped golden-path fixtures so docs, state, task boards, and QA reports stay aligned
- isolate compatibility-only roles from interactive agent-model setup and reinforce package-first docs across workflow, schema, and runbooks

## Validation

- `npm run verify:all`

## Published package

- npm: `@duypham93/openkit@0.3.3`

## Notes

- active workflow remains `Master Orchestrator -> Product Lead -> Solution Lead -> Fullstack -> Code Reviewer -> QA`
- package-first artifacts now have stronger runtime and governance backing while `brief/spec/architecture/plan` remain compatibility surfaces
