## What's changed

- materialize the missing project-root `.opencode/switch-profiles.js` wrapper used by the `/switch-profiles` in-session command
- materialize the matching `.opencode/openkit/switch-profiles.js` compatibility wrapper for managed workspace launches
- point both wrappers at the managed kit `src/runtime/switch-profiles-cli.js` entrypoint while preserving current-session `OPENKIT_RUNTIME_SESSION_ID` scoping

## Validation

- `node --test tests/cli/openkit-cli.test.js`
- `node --test tests/runtime/profile-switch.test.js`
- `node --test tests/global/ensure-install.test.js`

## Published package

- npm: `@duypham93/openkit@0.3.35`

## Notes

- Run `npm install -g @duypham93/openkit@latest` and then `openkit upgrade` so existing workspaces get the refreshed switch-profiles shim.
- This hotfix addresses `/switch-profiles` failing with `Cannot find module '<project>/.opencode/switch-profiles.js'`.
