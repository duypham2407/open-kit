## What's changed

- complete code-intelligence gap remediation phases 1-6, fixing 20 identified gaps (G01-G20) across the runtime analysis pipeline
- Phase 1 foundations: centralized source extensions, error visibility for indexing, FileWatcher wired into manager lifecycle
- Phase 2 index quality: parse-once optimization, callee_symbol_id in call graph, expanded caller coverage, lexical scope stack in reference tracker to suppress shadowed false positives
- Phase 3 language expansion: lightweight handlers for Python, Go, CSS, HTML, Markdown, YAML/TOML/JSON via regex-based extraction
- Phase 4 MCP dispatch: real builtin execution, external server dispatch (http/stdio), enriched specialists with system prompts and tool allowlists
- Phase 5 retrieval: FTS5 keyword search, batched cosine via typed-array matrix, import-scoped filtering for references and rename
- Phase 6 external tooling: tool-runner with timeout/PATH augmentation, tool.typecheck (tsc), tool.lint (eslint/biome), tool.test-run (vitest/jest/node:test/pytest/go test) — all self-gating and gracefully degradable

## Validation

- 385 runtime tests passed, 0 failed
- all invariants (I1-I7) preserved

## Published package

- npm: `@duypham93/openkit@0.3.20`

## Notes

- this release was prepared alongside the merge commit that resolved conflicts with the remote Phases 1-6 implementation
- release note file was retroactively created during the 0.3.21 release cycle
