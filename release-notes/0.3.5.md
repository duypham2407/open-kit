## What's changed

- remove deprecated brief/spec/plan surfaces from the active workflow and standardize the live artifact model around scope and solution packages
- auto-scaffold primary scope and solution markdown artifacts when work enters `full_product`, `full_solution`, and `migration_strategy`
- surface the latest auto-scaffolded artifact in runtime status and resume summaries so users and agents can immediately follow the generated document path
- move historical maintainer workflow-refactor notes out of the active AI-facing surface and into `docs/archive/maintainer/`

## Validation

- `npm run sync:install-bundle`
- `npm run verify:all`

## Published package

- npm: `@duypham93/openkit@0.3.5`

## Notes

- active workflow artifacts now center on `docs/scope/` and `docs/solution/` with auto-scaffold visibility recorded in workflow state
- historical workflow-refactor notes were archived to reduce AI-facing maintainer-surface noise without removing audit history
