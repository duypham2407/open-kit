## What's changed

- complete the OpenKit hybrid-runtime completion roadmap while keeping the workflow kernel authoritative
- deepen the additive runtime with richer config/model/category resolution, real hook behavior, session tooling, continuation control, browser verification, heuristic LSP tools, structured AST tools, and safer edit previews
- expand doctor, onboarding, command discovery, skill compatibility reporting, and registry metadata so the product path and compatibility path stay aligned with the implemented runtime

## Validation

- `npm run verify:all`

## Published package

- npm: `@duypham93/openkit@0.3.7`

## Notes

- workflow-state remains the source of truth; continuation, browser, LSP, AST, and safer-edit tooling stay additive and do not bypass approvals, evidence gates, or stage ownership
- some advanced tool families remain intentionally honest about degraded or preview-only behavior when full external dependencies are unavailable
