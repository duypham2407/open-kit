# Release Notes

This directory stores the per-version release notes used by `RELEASES.md`, the release workflow, and GitHub/npm release drafting.

Use `RELEASES.md` from the repository root as the human-friendly index.
