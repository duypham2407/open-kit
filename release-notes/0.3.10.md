## What's changed

- lock full-delivery planning to `Product Lead -> scope package -> Solution Lead -> solution package` so runtime state and prompts no longer allow premature parallel planning
- replace legacy full-lane wording across commands, agents, skills, operator docs, and install-bundle mirrors with the explicit scope-package and solution-package contract
- add explicit OpenCode permission defaults so normal non-destructive work runs without prompts while delete-style commands still require confirmation

## Validation

- `node --test tests/install/materialize.test.js && node --test tests/install/merge-policy.test.js && node --test tests/runtime/doctor.test.js && node --test tests/runtime/launcher.test.js && node --test tests/cli/openkit-cli.test.js && npm run verify:install-bundle`
- `node --test ".opencode/tests/workflow-state-controller.test.js" && node --test ".opencode/tests/work-item-store.test.js" && node --test ".opencode/tests/workflow-contract-consistency.test.js"`

## Published package

- npm: `@duypham93/openkit@0.3.10`

## Notes

- full delivery now requires the approved `Product Lead` scope package before `Solution Lead` can produce the solution package or enable parallelization
- if OpenCode remembers an `Always Allow` permission decision, that persistence still belongs to OpenCode rather than an extra OpenKit-owned approval store
