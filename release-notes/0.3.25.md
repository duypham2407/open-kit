## What's changed

- fix managed project installs so the generated root `opencode.json` registers the `openkit` MCP bridge by default
- add install coverage that locks the root manifest to include `mcp.openkit` and preserve the managed MCP contract during merge/materialization
- harden doctor workflow reporting so tool-enforcement plugin detection works from both repo-local and managed-kit runtime paths

## Validation

- `node --test tests/install/materialize.test.js tests/install/merge-policy.test.js`
- `node --test tests/mcp-server/mcp-server.test.js tests/runtime/mcp-dispatch.test.js`
- `node --test tests/runtime/doctor.test.js`
- `node --test tests/global/doctor.test.js`

## Published package

- npm: `@duypham93/openkit@0.3.25`

## Notes

- this patch closes the project-install gap where OpenCode could launch without the OpenKit MCP bridge even though the repo-local and global managed configs already declared it
