## What's changed

- **Structure reorganization**: All source code consolidated under `/src` directory
- **Runtime path update**: `.opencode/` renamed to `src/openkit-runtime/` to eliminate naming conflicts when OpenKit runs in its own repository
- **Test fixes**: Added structured scan evidence format for tool evidence gates
- Documentation remains at root level for discoverability

### Details

- All 11 source directories moved to `/src`: agents, assets, bin, commands, context, global, hooks, install, release, runtime, scripts, skills, tests
- Git history preserved through proper `git mv` operations
- Updated 476 files across the codebase
- All internal paths updated: package.json files array, registry.json component paths, CLAUDE.md references
- Fixed workflow state controller import paths and policy execution trace paths
- Updated release workflow paths for install manifest

## Validation

- All 1742 tests passing (verify:all, verify:governance, verify:semgrep-quality)
- Release workflow tests updated with new fixture structure
- Migration lifecycle test fixed with structured scan evidence
- ValidateState test fixed to use validateStateObject() directly
- Package metadata sync verified across package.json, package-lock.json, registry.json, install-manifest.json

## Published package

- npm: `@duypham93/openkit@0.9.0`

## Notes

- **Breaking change**: If you have hardcoded references to `.opencode/` paths in your project configuration, update them to `src/openkit-runtime/`
- **Compatibility**: The runtime shim at `.opencode/` (symlink or compatibility layer) ensures existing workflows continue to work during transition
- Git history preserved: Use `git log --follow` to trace file history across the reorganization
