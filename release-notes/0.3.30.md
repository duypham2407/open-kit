## What's changed

- harden capability status/readiness envelopes across MCP, code-intelligence, scan, syntax, codemod, and external tool surfaces
- add inspectable FEATURE-950 phase-completion evidence conventions without changing lane, gate, role, or parallel-execution semantics
- clarify operator and maintainer supported-surface docs for capability readiness, degraded states, fallback behavior, and target-project validation boundaries
- fix Semgrep rule-scan high-volume output classification so small buffered output is not misreported as degraded scan evidence

## Validation

- `npm run verify:all`
- `npm run verify:semgrep-quality`
- `node --test tests/runtime/audit-tools.test.js`
- Code Review pass and QA pass for FEATURE-950

## Published package

- npm: `@duypham93/openkit@0.3.30`

## Notes

- Capability-aware orchestration remains advisory only; this release does not add lanes, approval gates, hidden tool activation, or live parallel execution guarantees.
- Target-project application validation remains unavailable unless a target project defines app-native commands; OpenKit runtime checks must not be reported as target app proof.
