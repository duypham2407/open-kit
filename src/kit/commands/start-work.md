---
description: "Start execution from an approved plan or task board context."
---

# Command: `/start-work`

- Follow `context/core/prompt-contracts.md`.
- Follow `context/core/tool-substitution-rules.md` when reading or searching code before resuming execution.
- Use `context/core/runtime-surfaces.md` when deciding whether to rely on task-board state or session-only context.
- Use this command to continue approved execution work from explicit workflow-state context.
- Check saved continuation state, remaining actions, and stop reasons before resuming delegated or multi-session work.
