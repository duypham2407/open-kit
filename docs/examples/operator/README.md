# Operator Examples

This section collects operator-facing examples for supported daily use.

- Start here if you want the wrapper-first path for installing and running OpenKit.
- Treat these examples as non-authoritative walkthroughs that complement the live operator docs.
- When behavior differs, prefer `README.md`, `docs/operator/README.md`, and the current checked-in command surface.

Current operator example:

- `docs/examples/operator/2026-03-22-openkit-wrapper-install-and-run.md` is the primary first-run example for wrapper install, health checks, and launch.
